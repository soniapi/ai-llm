[package]
name = "ai_hypo"
version = "0.1.0"
edition = "2021"

[dependencies]
gcp_auth = "0.12.6"
prost = "0.13.5"
tokio = { version = "1.52.1", features = ["rt-multi-thread", "macros"] }
tonic = { version = "0.12.3", features = ["tls-roots"] }

[build-dependencies]
tonic-build = "0.12.3"
