[package]
name = "ai-llm-training"
version = "0.1.0"
edition = "2021"

[dependencies]
ai_infra = { path = "../ai-infra" }
ai-llm-inference = { path = "../ai-llm-inference" }
diesel = { version = "2.3.9", features = ["postgres"] }
